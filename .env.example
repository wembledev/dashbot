# DashBot environment variables
# Copy to .env and fill in values: cp .env.example .env

# --- Required ---

# Password for the initial admin user (used by db:seed)
DASHBOT_PASSWORD=

# Decrypts credentials.yml.enc in production
RAILS_MASTER_KEY=

# Alternative to RAILS_MASTER_KEY for PaaS deployments
# SECRET_KEY_BASE=

# API token for plugin authentication (Action Cable + REST)
DASHBOT_API_TOKEN=

# --- Optional ---

# Puma listen port (default: 3000)
# PORT=3000

# Puma thread pool size (default: 3)
# RAILS_MAX_THREADS=3

# Puma worker processes (default: 1)
# WEB_CONCURRENCY=1

# Run Solid Queue supervisor inside Puma (set to any value to enable)
# SOLID_QUEUE_IN_PUMA=1

# Solid Queue worker count (default: 1)
# JOB_CONCURRENCY=1

# Log verbosity: debug, info, warn, error, fatal (default: info)
# RAILS_LOG_LEVEL=info
